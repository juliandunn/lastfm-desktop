name: LastfmModern
options:
  createIntermediateGroups: true
settings:
  base:
    SWIFT_VERSION: 5.0
    CURRENT_PROJECT_VERSION: 1
    MARKETING_VERSION: 0.1.0
    GENERATE_INFOPLIST_FILE: YES
    PRODUCT_BUNDLE_IDENTIFIER: fm.last.scrobbler.modern
    PRODUCT_NAME: LastfmModern

targets:
  LastfmModern:
    type: application
    platform: macOS
    deploymentTarget: "13.0"
    sources:
      - path: Sources
      - path: Resources
        buildPhase: resources
    settings:
      base:
        PRODUCT_MODULE_NAME: LastfmModern
        ASSETCATALOG_COMPILER_APPICON_NAME: AppIcon
        INFOPLIST_KEY_CFBundleIconFile: AppIcon.icns
        INFOPLIST_KEY_CFBundleName: Last.fm modern
        INFOPLIST_KEY_CFBundleDisplayName: Last.fm modern
        INFOPLIST_KEY_LSApplicationCategoryType: public.app-category.music
        INFOPLIST_KEY_NSHumanReadableCopyright: Copyright (c) 2026

  LastfmModernTests:
    type: bundle.unit-test
    platform: macOS
    deploymentTarget: "13.0"
    sources:
      - path: Tests
    dependencies:
      - target: LastfmModern
    settings:
      base:
        PRODUCT_MODULE_NAME: LastfmModernTests
        GENERATE_INFOPLIST_FILE: YES
        PRODUCT_BUNDLE_IDENTIFIER: fm.last.scrobbler.modern.tests

schemes:
  LastfmModern:
    build:
      targets:
        LastfmModern: [run, test, profile, analyze, archive]
    test:
      targets:
        - LastfmModernTests
